\newcommand{\AbstractResults}{In a realistic scenario with late, missing, noisy, and shock-confounded outcomes and approval bias, consequence-weighted consolidation reached a decision quality of \val{realistic-why-ndq_tr} (0 = random choice, 1 = optimal), against \val{bestlimited-realistic-val} for the best other capacity-limited policy, captured \val{cap-realistic-why}\% of a counterfactual oracle's headroom over random retention, and beat the recency, similarity, recurrence, self-rated-importance, and user-preference baselines on every seed. Under a self-report poisoning campaign it lost \val{dmgabs-poison-why} in decision quality while self-rated importance lost \val{dmgabs-poison-importance}. The support is conditional. With immediate, trustworthy outcomes the advantage over random retention shrinks to \val{pair-clean-random-ndq_tr}. Ablations credit waiting for outcomes, discounting manipulable channels, and detecting confounded days rather than surprise or stakes, and the surprise factor is what makes the method worse calibrated. When poisoned outcomes pass verification, the method admits more poison than random retention. Unbounded memory beats it in every stationary scenario but collapses when the world changes (\val{world_change-keep_all-ndq_tr} versus \val{world_change-why-ndq_tr}).}
